<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#000">

    <ImageView
        android:id="@+id/user_comment_imz"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/username_comment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:text="user_name"
        android:textColor="#fff"
        app:layout_constraintStart_toEndOf="@id/user_comment_imz"
        app:layout_constraintTop_toTopOf="@id/user_comment_imz" />

    <TextView
        android:id="@+id/user_comment_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:textColor="#fff"
        app:layout_constraintStart_toStartOf="@id/username_comment"
        app:layout_constraintTop_toBottomOf="@id/username_comment" />

</androidx.constraintlayout.widget.ConstraintLayout>